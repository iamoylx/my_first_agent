# 小满 Agent 进化路线规划（plan.md）

> 目标：把「小满」从"桌面被动问答"升级为"常驻生活陪伴助手"——
> 本地模型、主动触发、生活/健康 skill、MCP 数据源、微信接入。
> 本文件是路线图 + 决策记录；标注"待决策/分支"的条目为长期选项，先不实施。
>
> **当前进度（2026-08-13）**：阶段0/A1/A2/记忆v3/B1/A3/A4/A4.2 已完成；
> 下一步：B2 健康数据（随手表决策）→ C 系列（微信/常驻化）。

---

## 0. 设备现状（已实测）

| 项 | 值 | 含义 |
|---|---|---|
| GPU | **RTX 4070 Laptop 8GB**（当前被 WebView2 占 ~3.4GB） | 本地模型选 7B/8B Q4 |
| CPU | i9-14900HX（24C/32T） | 强 |
| 内存 | 16GB | 8B 模型需 GPU+RAM 混合，够用 |
| 磁盘 | D 盘余 176GB | Ollama+模型 ~5GB，充足 |
| 操作系统 | Windows | 全屏检测用 Win32 API |

**本地模型结论**：装 **Ollama**，模型选 **Qwen3-8B（Q4_K_M，~5GB，~30-40 tok/s）**或更稳的 **Qwen2.5-7B-Instruct（Q4，~4.7GB）**。
- 选聊天模型而非 DeepSeek-R1 蒸馏（R1 是推理模型，不适合闲聊陪伴）
- Qwen3-8B 支持工具调用；若工具调用不稳，采用"日常闲聊走本地 / 复杂任务走 DeepSeek API"双模型策略

---

## 阶段 0 —— 思考过程可视化（最高优先级，最早实现）⭐ ✅ 已完成 2026-08-11

> 目标：像 Codex 一样，把「小满的思考过程」实时呈现给用户——
> 可折叠的「💭 思考中」标签，展开看灰色思考/决策轨迹。
> 这既是体验升级，更是后续优化与 bug 定位的"黑匣子"。

### 0.1 后端：思考轨迹捕获（Thinking Trace）
- `core/agent_core.py` 的 `process_turn` 增加 `on_thinking` 回调，关键节点推送结构化轨迹：
  - `[记忆]` 刷新档案卡：读取 N 条生效事实 / 命中最近摘要
  - `[工具]` 检测到工具调用：工具名 + 参数摘要（值脱敏/截断）
  - `[工具]` 工具执行完成：结果摘要（截断）
  - `[防御]` DSML 工具调用标记被识别并纠正（工具能跑通的关键节点）
  - `[模型]` 开始生成最终回答
- 模型真实推理（增强项，需实测）：
  - DeepSeek thinking 模式 / Qwen3 thinking 的 `reasoning_content` 从流式 delta 捕获
  - 若 LangChain `ChatOpenAI` 不透出该字段 → 降级用底层 openai SDK 直连或 `astream_events` 提取
  - 捕获不到也不阻塞：至少展示执行轨迹
- **与现有流程零冲突**：只加回调，不改消息结构、不改记忆写入

### 0.2 协议（第一版走最小改动，最快上线）
- 第一版：`POST /chat` 响应加 `"thinking": [...]`（轨迹数组，时间序）——前端一次性拿到，改动最小
- 演进版：`/chat/stream` SSE 增加 `{"type":"thinking", ...}` 事件 → 思考过程逐条/逐字实时刷新（真流式）

### 0.3 前端
- AI 回复上方显示「💭 思考中」可折叠条（灰色小字），点击展开/收起思考轨迹
- 等待回复时：显示「思考中...」+ 可提前展开查看已到达的轨迹
- 样式：与正文明显区分（灰字、小号、折叠态只占一行）
- 桌宠模式：回复时显示「思考中」小气泡动画即可，不展开细节（细节在主窗口对话看）

### 0.4 思考轨迹落盘（黑匣子）
- 轨迹写入 `logs/thinking-YYYYMMDD.jsonl`（**不进 memory_data，不污染记忆**）
- 作用：人设跳脱 / 工具调用异常 / DSML 死循环 / 本地模型切换，都有迹可循

### 0.5 收益
- 用户可见 agent 决策过程 → 信任感与可解释性
- 工具调用透明化 → 排查"为什么调用/没调用某工具"
- 后续本地模型接入时，能直接观察是否思考、是否走工具、思考质量如何

---

## 一、阶段 A —— 可立即实现（纯本地、无外部依赖）

### A1. 主动触发机制（核心体验升级）✅ 已完成 2026-08-11
- Python 后端加 asyncio 调度器，读档案作息/待办生成提醒（23:00 该睡/睡前牛奶、健身/电影计划到点）
- **载体**：桌宠主动弹气泡 + Windows 通知
- **打扰规则（用户已定）**：不免打扰；仅当前台有**全屏程序**时不打扰
  - 全屏检测：Win32 `GetForegroundWindow` + 与屏幕尺寸比对（轮询即可，成本低）
- 参考案例：QClaw 私人教练定时监督、飞书 AI 活动提醒 agent、proactive-nudge（OpenClaw 生态）

### A2. 生活类 skill（注册即生效）✅ 已完成 2026-08-11（reminder/todo 提醒任务 ✅ / weather 天气 ✅ / health_record 健康记录 ✅；客户端图片附件多模态预留 ✅）
- `weather`：专门城市天气工具（重邮/江西，替代泛搜）
- `reminder` / `todo`：存待办 → 主动触发用
- `health_record`：手动记录睡眠/体重/步数 → 进 MemoryStore `events`（为健康 MCP 打底）

### A3. 本地模型接入（Ollama）✅ 已完成 2026-08-12（gemma3:4b 多模态：图片走本地视觉，文本走 DeepSeek 双模型；detect 对不支持工具的模型自动降级）
- `ollama pull qwen3:8b`，LangChain 的 `ChatOpenAI` 换 `base_url=http://127.0.0.1:11434/v1` + `AGENT_MODEL` 环境变量
- 双模型切换：`AGENT_LLM_BASE` 决定走本地还是云端；工具调用弱时回落 DeepSeek

---

## 二、阶段 B —— 近期可实现（取决于手表决策）

### B1. 通用 MCP Client 框架 ✅ 已完成 2026-08-12（mcp_bridge 配置式 client + 工具动态注册 + /mcp/tools；Obsidian 笔记已接入并实测模型多步调用；企业微信推送 Carrier 已接（WECOM_WEBHOOK_URL）；browser_use/music/calendar 配置模板就绪，待凭证/启用）
- 用 `mcp` Python SDK 做配置式 MCP 客户端，小满作为 MCP 客户端调用外部数据源
- 数据进 MemoryStore（events），小满据此主动关心

### B2. 健康数据源（**待决策：买哪款手表**，见第四节）⏳ 待决策
- 可行路径（按现实难度排序）：
  1. 手动/导入记录（立刻能做，无需手表）
  2. 华为：`huawei-health-mcp`（开源，但需华为 Health Kit 开发者审批）
  3. Amazfit/华米 Zepp（有开发者平台）、小米（非官方导出）
  4. Apple Health（需苹果设备）
- 华强北手表：**Wearfit App 无公开 API，数据难接入 MCP**（见第四节）

---

## 三、阶段 C —— 需要决策 / 外部依赖（长远打算，先不实施）

### C1. 微信接入（**待决策**，方案对比见第五节）
- 优先级建议：**企业微信（企微）> 已认证服务号 > 个人订阅号 > 个人微信协议（高风险）**

### C2. 后台常驻服务化
- 主动触发/微信/定时都要求后端常驻（现在桌面关了就没了）
- 演进：小满变成常驻个人助手服务，桌面客户端为前台 UI；开机自启 + 通知/推送

### C3. 多端界面
- 手机端（配合微信/企微）、Web 面板（会话/档案/健康可视化）

---

## 四、设备决策记录 —— 智能手表（**待决策**）

### 华强北 Apple Watch（乔帮主 HK10 Pro+ 实测）
- 优点：外观像 Apple Watch、大屏、便宜
- 缺点（已核实评测）：
  - 心率与 Apple Watch 差距 ±2-3（尚可）；睡眠可看
  - **血压/血糖不可信**（血糖无测量过程直接显示结果；均无医疗器械认证）
  - 配 **Wearfit Pro App，无公开 API → 数据难接入 MCP**
- 结论：追求"好看"可以买来玩，但**若目标是健康数据接入小满，华强北不合适**

### 300 元档主流（更有 MCP 潜力）
| 型号 | 价格 | 备注 |
|---|---|---|
| 荣耀手表 X5i | 249-254 | 1.97" AMOLED、心率/血氧/睡眠/压力、性价比高 |
| Amazfit 米动手表青春版 Lite | 289 | 华米/Zepp 生态，开发者平台较友好 |
| 小米手环 9 Pro | 279 | 小米生态，数据导出有非官方方案 |
| 荣耀手环 10 | 169-249 | 最便宜 |

- **倾向建议**：若选"能接入数据"，优先 **Amazfit（Zepp）或小米**；若只是想要个好看的，华强北可以但数据接不了

---

## 五、微信接入方案对比（**待决策**）

| 方案 | 体验 | 主动推送 | 风险/成本 | 结论 |
|---|---|---|---|---|
| **企业微信（企微）** | 群里 @ 机器人/个人会话 | ✅ 定时消息 | 合规、免费注册 | **首选**：稳定，支持定时/文件/多模型 |
| 已认证服务号 | 公众号对话 | ✅ 客服消息 | 需企业主体认证 | 合规，主动推送要认证 |
| 个人订阅号 | 公众号对话 | ❌ 只能 15s 被动回复（可 hack 缓存） | 免费，免费注册 | 可做一次性问答，体验一般 |
| **个人微信协议**（wechaty/itchat/逆向） | 个人微信直聊，体验最好 | ✅ | **有封号风险**（见下） | 高风险实验分支 |

**关于个人微信封号（用户问题 3 的答案）**：
- **只是自己低频用，依然有封号风险**——微信检测的是"非官方协议"本身，与内容无关；低频/自用只是降低触发概率，不能消除（近期大量用户弹出外挂警告、协议失效）
- **不建议买空微信号**：a) 出售/购买微信号存在法律风险（司法案例：买卖空白微信号可构成"侵犯公民个人信息罪"）；b) 空号多来自黑产养号，本身容易被风控封禁
- 结论：先走企微/服务号合规线；个人微信协议只作为实验，不依赖

---

## 六、资料与信息来源

- 本地模型：PromptQuorum《为你的 GPU 选择最佳 DeepSeek 蒸馏模型（2026）》（8GB 档）；smeltcore Qwen3-8B on RTX 4070
- 华强北手表：我爱音频网《乔帮主 HK10 Pro+ 评测》（血压/血糖不可信、Wearfit 无 API）
- 300 元手表：什么值得买/今日头条 2026 评测（荣耀 X5i、Amazfit Lite、小米手环9 Pro）
- 微信：cocoloop《微信机器人接入 AI 三种方案对比》；dirk1983/ai-wechat-personal（订阅号 15s hack）；GitHub wechaty 系列封号警告；最高人民法院 买卖微信号案例
- MCP：SabaTech-dev/huawei-health-mcp（华为健康 MCP 开源）
- 主动触发：QClaw 私人教练案例、proactive-nudge（OpenClaw 生态）

---

## 七、待你拍板的决策点

1. [ ] 手表：华强北（好看、数据接不了） vs Amazfit/小米/荣耀（能接数据）？还是先买便宜的玩？—— **仍未买，B2 待此决策**
2. [x] 微信：先做企微（合规稳定）—— **已定**：企微推送 Carrier 已接（WECOM_WEBHOOK_URL）；对话入口/服务号/个人微信暂缓
3. [x] 本地模型：已换 **qwen3-vl:4b**（多模态 + 中文 + **原生工具调用**，~3.3GB，D 盘，Ollama 已导入）—— **已定**；gemma3 因无专用工具 token 已卸载。客户端 DeepSeek/本地 一键切换 ✅
4. [x] 主动触发内容范围：已做 **作息睡眠/健身/睡前牛奶/天气带伞/空闲关心**（Clock/Reminder/Idle 源）—— 待办/健康待 A2 增强或 B2

> 执行顺序建议：**阶段0✅ → A1✅ → A2✅ → B1(MCP+Obsidian+企微)✅ → 记忆v3✅ → 瘦身✅ → A3(本地模型)✅已完成 → B2(健康数据，随手表)⏳ → C(微信/常驻化)⏳**
>
> 后续章节：八、项目现状总览 / 九、未完成事项 / 十、后续功能建议 / 十一、性能优化方向

---

## 八、项目现状总览（2026-08-12）

| 模块 | 现状 | 关键文件 |
|---|---|---|
| 思考可视化（阶段0） | ✅ 完成 | `core/agent_core.py`（on_thinking/on_reasoning）、前端折叠块、`logs/thinking-*.jsonl` |
| 主动触发（A1） | ✅ 完成（v2） | `active/`（scheduler/sources/policy/carriers）、桌宠气泡/主窗口/企微推送、全屏免打扰；**通用「时间+内容」+ 重复模式**（每天/周X/工作日/周末/每N天/日期） |
| 生活 skill（A2） | ✅ 完成 | `skills/`（reminder_tools/weather/health_record） |
| 图片附件（A2.5） | ✅ 完成 | 前端选图/粘贴 → base64 → `logs/uploads/`（多模态预留） |
| 记忆 v3 板块化 | ✅ 完成 | `memory/`（5 板块 + 自动分类/命名/去重 + 前端折叠编辑）；迁移 `scripts/migrate_profile_v3.py` |
| MCP 框架（B1） | ✅ 完成 | `mcp_bridge/` + `mcp/config.json`（Obsidian 已接入，browser-use/音乐/日历模板就绪） |
| 企业微信推送 | ✅ 完成 | `active/carriers.py` WeComCarrier（`WECOM_WEBHOOK_URL`） |
| 桌面客户端 | ✅ 可用 | Tauri 双窗口 + 桌宠 6 状态 + 记忆 UI + 附件；根目录 exe 发布 |
| 项目结构 | ✅ 瘦身完成 | 扁平目录；备份/旧测试归档 `D:\document\AGENT_archives\`；回归 `scripts/selfcheck/` |
| 本地模型（A3） | ✅ 完成 | Ollama（v0.32.9）+ **qwen3-vl:8b**（D 盘，~6.2GB）；DeepSeek/本地 一键切换；本地模式 8B 重开工具（文本有工具、看图视觉聚焦）；`OLLAMA_KEEP_ALIVE=10m` 闲置自动卸载 |
| 健康数据（B2） | ⏳ 待决策 | 等手表（华为/Amazfit/小米）；`health_record` skill 已打底 |
| 微信对话入口（C1） | ⏳ 未做 | 企微推送已有；企微"聊天入口"/服务号/个人微信未做 |
| 后台常驻（C2）/多端（C3） | ⏳ 未做 | 常驻服务化 + 手机/Web 界面 |

---

## A4. 语音朗读（TTS）——「🔊 朗读」按钮 ✅ 已完成 2026-08-13

> 设计（用户拍板）：每条 AI 消息下方一个「🔊 朗读」小灰按钮，**点按才调用**——不点不调用、
> 不常驻任何 TTS 引擎/进程，零资源占用。在线走 edge-tts（免费、零显存）；断网/失败明确提示。

### A4.1 已实现（edge-tts 在线）
- 后端 `POST /tts`（`desktop-client/agent-server.py`）：edge-tts 流式合成 → base64 mp3 返回
  - 默认音色 `zh-CN-XiaoxiaoNeural`（晓晓，甜美女声，贴合小满）；支持传 voice 换音色
  - 文本上限 3000 字、60s 超时、空文本 400、失败透传真实错误
- Rust `tts_speak` command（`desktop-client/src-tauri/src/commands.rs`）+ `main.rs` 注册
- 前端：`appendMessage` / `typeMessage` 后给 AI 消息加 `🔊 朗读` 按钮（历史消息也可朗读）
  - 状态流转：朗读 → 合成中… → 播放中… → 朗读；失败/断网明确提示
- 依赖：`pip install edge-tts`（已装 7.2.8）

### A4.2 B 计划（Qwen3-TTS 本地引擎）✅ 已完成 2026-08-13
> 目标达成：断网也能用小满专属音色；有情感、不读 emoji。
- 模型：`Qwen/Qwen3-TTS-12Hz-0.6B-CustomVoice`（~2.3GB，D:\models\qwen3-tts-0.6b-customvoice，hf-mirror 镜像下载）
- 引擎能力：`generate_custom_voice(text, speaker, language, instruct)` —— **instruct 自然语言情感指令** + 9 个预置音色（vivian/serena/ono_anna 等女声）+ 未来 VoiceClone 免训练克隆
- 落地方式（已实现）：
  1. `desktop-client/tts_server.py`：独立进程（端口 18900），模型懒加载 + 空闲 600s 自动退出释放显存
  2. `agent-server.py /tts` 引擎抽象 `engine: "auto"|"edge"|"local"`：auto 检测本地模型存在 → 懒启动本地引擎；失败/断网回退 edge
  3. **emoji/markdown/URL 预处理**（`_tts_clean_text`，两引擎统一）：💖☀️😭 等全部清除，不再读符号
  4. **情感注入**（`_tts_instruct`）：开心/难过/关切/默认四类启发式 → 中文语气指令（如「用开心甜蜜的语气，带着笑意」）
  5. 默认音色 **serena**（用户试听后选定；vivian/serena/ono_anna 试听 `tmp/tts_*.wav`），`QWEN3_TTS_SPEAKER` 可换
- 音色克隆（已接入 ✅）：参考音频 `D:\models\甜美小雪_晚安语音.wav`（豆包生成 85s）+ 原文 → `D:\models\qwen3-tts-clone.json` → tts_server 克隆模式，日常「🔊 朗读」全用专属音色
  - 克隆用 **1.7B-Base**（`D:\models\qwen3-tts-1.7b-base`，~4.5GB）：0.6B 电子味重，1.7B 明显自然（用户确认）
  - 合成参数：temperature 0.6 / top_k 30（用户试听选定：重音自然，如「我好想你」重音正确落在「想」）
  - 工具：`scripts/voice_clone.py` 试效果；`scripts/download_hf_direct.py` 直连下载（绕过 snapshot_download 卡顿）
  - 换音色：替换 ref 音频 + 改 config 即可，免训练随时换
- 性能实测（4070 8G）：首次点朗读（含模型加载）~30-50s，之后每次 ~26s/句；与本地 LLM 错峰（模型加载时 qwen3-vl 会释放部分显存）
- 待办（可选）：前端音色选择器；VoiceClone 用 10~30s 参考音频克隆「小满专属音色」

### A4.3 C（用户不优先，暂缓）：GPT-SoVITS v4 专属音色训练 ⏸️ 不考虑
- 推理 FP16 ~5G 显存；训练仅能 LoRA（8G 卡跑不了全量 14G）
- 训练流程：录 5~10min 干净人声 → 自动切割标注 → 特征提取 → GPT+SoVITS LoRA 训练（4070 约 30~60min）
- 质量天花板但训练成本重；等 B 验证体验后再议

---

## 九、未完成事项（按优先级）

1. ~~A3 本地模型接入~~ ✅ 已完成：LangChain → Ollama qwen3-vl:4b（原生工具调用 + 看图）；客户端 DeepSeek/本地 切换按钮（frontend + Rust provider 透传 + `_route_llm` 路由）；模型导入脚本 `D:\ollama\download_qwen3vl.py` + `Modelfile-qwen3vl`
2. **B2 健康数据**：等手表决策 → huawei-health-mcp / Zepp / 小米（`HealthSource` 填进现成 TriggerSource 接口）
3. **C1 微信对话入口**：企微应用消息（可双向聊天）/ 服务号 / 个人微信（高风险）
4. **C2 后台常驻服务化**：小满变成常驻服务（开机自启 + 通知），桌面为前台 UI
5. **C3 多端界面**：手机端（配合企微/微信）、Web 面板（会话/档案/健康可视化）
6. **MCP 启用项**：browser-use（需 `playwright install chromium` + LLM key）、音乐（扫码登录）、日历（飞书 OAuth）——模板就绪，配好即用
7. ~~A4.2 B 计划~~ ✅ 已完成：Qwen3-TTS-0.6B-CustomVoice 本地引擎（有情感 + 去 emoji + 懒加载 + 空闲卸载）；待办：前端音色选择器 / VoiceClone 专属音色
8. **小增强**：Windows 通知 Toast（装 winotify 启用）、思考可视化真流式（SSE 前端接入）、记忆 value 级模糊相似索引、桌宠气泡点击联动主窗口

---

## 十、后续功能建议（新）

| 功能 | 说明 | 依赖 |
|---|---|---|
| **每日早安简报** | 桌宠每天推送：天气 + 日程/待办 + 提醒汇总（天气 skill + reminder + 日历） | 本地即可做 |
| **语音交互**（TTS/STT） | 桌面端语音对话，桌宠更像陪伴；Windows SAPI 或本地模型 TTS | 本地模型 |
| **学习助手 skill** | 重邮课表导入 + 上课/考试/DDL 提醒；笔记回顾（Obsidian MCP 已接） | 本地 |
| **游戏助手** | 鸣潮/三角洲/洛克王国：版本更新/活动提醒、攻略速查 | 本地/搜索 |
| **截图/OCR** | 截屏分析（配合 gemma3 多模态 + 图片附件链路） | A3 之后 |
| **习惯/健康可视化** | health_record 数据 → 趋势图（Web 面板/主窗口） | B2 之后 |
| **智能家居**（HA MCP） | 宿舍可能用不上，毕业/家里再考虑 | 设备 |
| **主动场景扩展** | 结合日程提前准备（会前摘要、出行前天气+路线） | 日历 MCP 后 |

---

## 十一、性能优化方向

| 方向 | 现状 | 优化建议 |
|---|---|---|
| **记忆上下文预算** | 档案卡全文注入（39 条，≤15% 预算） | 板块按需注入/摘要化；长会话滚动摘要 + 关键片段保留 |
| **模型调用次数** | 已优化 detect 直接采用（省一次调用） | 相似问题缓存命中；复杂任务才走 DeepSeek（本地模型分流） |
| **MCP 连接** | 启动时 npx 串行连接（~3-6s/个） | 连接池/懒加载；可用后常驻复用 |
| **图片传输** | base64 原图（≤10MB） | 前端压缩/降采样后再传；服务端缩略图 |
| **前端长会话** | 消息全量渲染 | 虚拟滚动（只渲染可视区） |
| **主动触发 tick** | 20s 轮询 + ctypes 全屏检测 | 事件驱动（WinEventHook）替代轮询；检测结果缓存 |
| **Ollama 推理** | — | Q4 量化 + `num_ctx` 控制 + KV cache + `OLLAMA_KEEP_ALIVE` 常驻 |
| **日志** | jsonl 无限追加 | 按大小/天数轮转 |
| **启动速度** | MCP + 记忆恢复串行 | 并行化启动；MCP 失败快速降级 |
| **token 成本** | finalize 抽取+摘要已并发 | 增量摘要（每 N 轮）；低置信度抽取降频 |
